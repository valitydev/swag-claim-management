type: object
required:
  - documentId
  - documentModification
properties:
  documentId:
    description: Идентификатор документа
    type: string
    maxLength: 40
    minLength: 1
  documentModification:
    x-rebillyMerge:
      - $ref: '#/definitions/DocumentModification'
